#include "main.h"
/**
 * print_numbers- prints the numbers
 *
 * Return: Always Returns 0
 */
void print_numbers(void)
{
	int a = 0;
	while (a < 10)
	{
		putchar(a);
		a++;
	}
	putchar('\n');

	return (0);
}
