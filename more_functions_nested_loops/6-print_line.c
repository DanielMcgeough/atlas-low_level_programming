#include "main.h"
/**
 * print_line- make the function print something
 * @n: input number
 *
 * Return: return is void
 */
void print_line(int n)
{

	while (n > 0)
	{
		_putchar((95 + n)'\n')
		if (n <= '0')
		{
			_putchar('\n');
		}
	}
	return (0);
}
