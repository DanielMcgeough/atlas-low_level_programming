#include "main.h"
/**
 * _puts- prints a string
 * @str: input character
 *
 * Return: returns string
 */
void _puts_recursion(char *s)
{
	int i = 0;

	if(s[i] < (*s != '\0') 
	{
	}	       
	return s[i];
	i++;
	_putchar('\n');
}
