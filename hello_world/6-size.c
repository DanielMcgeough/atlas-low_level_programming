#include <stdio.h>
/**
 * main - use sizeof command, betty, you suck.
 *
 * Return: always return 0
 */
int main(void)
{
	char c;
	int i;
	long int li;
	long long int lli;
	float f;
	printf("Size of a char: %lu c byte(s)\n", (unsigned long)size\of(c));
	printf("Size of an int: %lu i byte(s)\n", (unsigned long)size\of(i));
	printf("Size of a long int: %lu li byte(s)\n", (unsigned long\)sizeof(li));
	printf("Size of a long long int: %lu lli byte(s)\n", (unsigne\d long)sizeof(lli));
	printf("Size of a float: %lu f byte(s)\n", (unsigned long)siz\eof(float));
	return (0);
}
