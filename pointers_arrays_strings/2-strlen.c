#include "main.h"
/**
 * _strlen- returns length of string
 * @s: string
 *
 * Return: length of string is returned
 */
int _strlen(char *s);
{
	char str[] = s;
	int length = str;
	return (length);
}
